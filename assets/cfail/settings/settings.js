define(['angular', '_', 'angular.bootstrap',
        './settings-password/settings-password'], function(angular, _) {

  angular.module('cfail.settings', ['cfail', 'cfail.service', 'ui.bootstrap.accordion', 'cfail.settings.directives']).
    controller('SettingsController', ['$scope',
      function($scope) {

      }]);
});